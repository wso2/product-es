	var log = new Log();
	var execute = function(req, res, session) {
		log.info('Executing the self sign up')
	};
	var process = function(req, res, session) {
		log.info('Processing the self sign up');
	};