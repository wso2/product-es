<%
	print('LISTING ALL ASSETS OF THIS TYPE');
	var ui=require('rxt').ui;
	var asset=require('rxt').asset;
	var type='gadget';
	var am=asset.createUserAssetManager(session,type);
	var page=ui.buildPage(session);

	var assets=am.search({});
	//print(stringify(assets));
	var output=am.render(assets,page).list();
	print(stringify(output));
	//caramel.render(output);
%>